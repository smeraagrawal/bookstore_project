<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}Bookstore{% endblock %}</title>
    <style>
        body { font-family: sans-serif; margin: 20px; background-color: #f4f4f4; color: #333; }
        nav { margin-bottom: 20px; background-color: #333; padding: 10px; border-radius: 5px; overflow: hidden;}
        nav a { margin-right: 15px; text-decoration: none; color: #fff; padding: 10px 15px; display: block; float: left;}
        nav a:hover { background-color: #555; }
        .flash { padding: 15px; margin-bottom: 20px; border-radius: 4px; border: 1px solid transparent; }
        .flash.success { background-color: #d4edda; color: #155724; border-color: #c3e6cb; }
        .flash.danger { background-color: #f8d7da; color: #721c24; border-color: #f5c6cb; }
        .flash.info { background-color: #d1ecf1; color: #0c5460; border-color: #bee5eb; }
        .flash.warning { background-color: #fff3cd; color: #856404; border-color: #ffeeba; }
        table { border-collapse: collapse; width: 100%; margin-bottom: 20px; background-color: #fff; box-shadow: 0 2px 3px rgba(0,0,0,0.1); }
        th, td { border: 1px solid #ddd; padding: 12px; text-align: left; }
        th { background-color: #e9ecef; color: #495057; }
        tr:nth-child(even) { background-color: #f8f9fa; }
        tr:hover { background-color: #f1f1f1; }
        form { margin-bottom: 15px; }
        button, input[type=submit] { padding: 8px 15px; cursor: pointer; border-radius: 4px; border: none; color: white; }
        button[type=submit] { background-color: #007bff; }
        button[type=submit]:hover { background-color: #0056b3; }
        .add-form { border: 1px solid #ccc; padding: 20px; margin-top: 20px; background-color: #fff; border-radius: 5px; box-shadow: 0 2px 3px rgba(0,0,0,0.1);}
        .add-form h3 { margin-top: 0; }
        .add-form label { display: block; margin-bottom: 5px; font-weight: bold; }
        .add-form input[type=text],
        .add-form input[type=number],
        .add-form input[type=password],
        .add-form input[type=float] { width: calc(100% - 20px); padding: 10px; margin-bottom: 15px; border: 1px solid #ccc; border-radius: 4px; box-sizing: border-box; }
        .action-button { background-color: #dc3545; }
        .action-button:hover { background-color: #c82333;}
        .edit-button { background-color: #ffc107; color: black; margin-right: 5px;}
        .edit-button:hover { background-color: #e0a800; }
        .buy-button { background-color: #28a745; }
        .buy-button:hover { background-color: #218838;}
        .nav-logout { float: right !important; }
        main { background-color: #fff; padding: 20px; border-radius: 5px; box-shadow: 0 2px 3px rgba(0,0,0,0.1); }
        h2 { color: #333; border-bottom: 2px solid #007bff; padding-bottom: 5px; margin-bottom: 20px;}
        ul { list-style-type: none; padding: 0; }
        ul li { margin-bottom: 10px; }
        ul li a { text-decoration: none; color: #007bff; }
        ul li a:hover { text-decoration: underline; }
        .form-inline { display: inline; }
        .form-inline button { margin-left: 5px; }
    </style>
</head>
<body>

    <nav>
        <a href="{{ url_for('home') }}">Home</a>
        {% if session.user_type == 'admin' %}
            <a href="{{ url_for('admin_dashboard') }}">Admin Dashboard</a>
            <a href="{{ url_for('admin_manage_books') }}">Manage Books</a>
            <a href="{{ url_for('admin_manage_authors') }}">Manage Authors</a>
            <a href="{{ url_for('admin_manage_staff') }}">Manage Staff</a>
            <a href="{{ url_for('admin_manage_customers') }}">Manage Customers</a>
            <a href="{{ url_for('admin_view_reports') }}">View Reports</a>
        {% elif session.user_type == 'customer' %}
            <a href="{{ url_for('customer_dashboard') }}">Customer Dashboard</a>
            <a href="{{ url_for('customer_view_books') }}">View Books</a>
            <a href="{{ url_for('customer_purchase_history') }}">Purchase History</a>
        {% endif %}

        {% if session.user_type %}
            <a href="{{ url_for('logout') }}" class="nav-logout">Logout ({{ session.user_id if session.user_type == 'admin' else session.cust_id }})</a>
        {% else %}
             <a href="{{ url_for('login') }}" class="nav-logout">Login</a>
        {% endif %}
    </nav>

    <main>
        {% with messages = get_flashed_messages(with_categories=true) %}
          {% if messages %}
            <div>
            {% for category, message in messages %}
              <div class="flash {{ category }}">{{ message }}</div>
            {% endfor %}
            </div>
          {% endif %}
        {% endwith %}

        {% block content %}{% endblock %}
    </main>

    </body>
</html>